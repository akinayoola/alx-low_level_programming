#include "main.h"
#include <stdio.h>

/**
 * _abs - computes the absolute value of an integer
 * @a: - number to be determined
 *Return: 0
 */

int _abs(int a)
{
        if (a < 0)
        {
                return (-a); }
        return (a);
}
