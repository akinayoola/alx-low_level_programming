#include "main.h"

/**
 *_isupper - print uppercase character
 *@c: number to be determined
 *
 * Return: 1 if uppercase and 0 if otherwise
 */

int _isupper(int c)
{
        if ('c' == 65 && c >= 90)
                return (1);
        else
                return (0);
}
